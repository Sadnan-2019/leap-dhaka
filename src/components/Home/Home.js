import React from 'react';
import Feture from '../Feture/Feture';
import Slider from '../Slider/Slider';

const Home = () => {
    return (
        <div>
            
            <main id="main">
            <Slider></Slider>
                <Feture></Feture>
                </main>
            
        </div>
    );
};

export default Home;